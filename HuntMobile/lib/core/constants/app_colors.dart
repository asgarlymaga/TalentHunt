import 'package:flutter/material.dart';

/// App-wide design system constants matching the mobile mockup palette
/// and "ticket/index-card" aesthetic.
class AppColors {
  /// Primary background - warm bone / off-white tone
  static const Color bone = Color(0xFFF7F5F0);

  /// Dark contrast text & accents - deep ink
  static const Color ink = Color(0xFF1E2022);

  /// Primary action / accent - forest green
  static const Color forestGreen = Color(0xFF2D5A27);

  /// Secondary action / highlight - clay red / warm terracotta
  static const Color clayRed = Color(0xFFC84B31);

  /// Card surface color - pure white ticket background
  static const Color cardSurface = Colors.white;

  /// Muted gray for borders, chips, and inactive elements
  static const Color mutedGray = Color(0xFFE2E0D8);

  /// Subtitle / caption text color
  static const Color textMuted = Color(0xFF686D76);
}
